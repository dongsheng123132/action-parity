/* eslint-disable */
/**
 * Generated by ActionParity from the executable Action Registry.
 * DO NOT EDIT. Change the Registry and regenerate this file.
 */

export const ACTION = {
  /** Mark one task as done. Completing an already done task changes nothing. */
  TASK_COMPLETE: "task.complete",
  /** Add one task to the board. */
  TASK_CREATE: "task.create",
  /** List every task on the board. */
  TASK_LIST: "task.list",
  /** Permanently delete every completed task. */
  TASK_PURGE: "task.purge",
} as const;

export type ActionId = (typeof ACTION)[keyof typeof ACTION];

export type ActionInputMap = {
  "task.complete": { id: string; };
  "task.create": { tags?: Array<string>; title: string; };
  "task.list": { done?: boolean; };
  "task.purge": Record<string, never>;
};

export type ActionOutputMap = {
  "task.complete": { core_execution_id: string; state_version: string; task: { done: boolean; id: string; tags: Array<string>; title: string; }; };
  "task.create": { core_execution_id: string; task: { done: boolean; id: string; tags: Array<string>; title: string; }; };
  "task.list": { core_execution_id: string; state_version: string; tasks: Array<{ done: boolean; id: string; tags: Array<string>; title: string; }>; };
  "task.purge": { core_execution_id: string; removed: Array<string>; state_version: string; };
};

export type ActionRequest<A extends ActionId = ActionId> = {
  action_id: A;
  input: ActionInputMap[A];
  confirmed: boolean;
  execution_id?: string;
  surface?: string;
};

export type ActionError = {
  class: string;
  code: string;
  message: string;
  details?: unknown;
};

export type ActionEnvelope<A extends ActionId = ActionId> =
  | {
      ok: true;
      version: number;
      action_id: A;
      execution_id: string;
      result: ActionOutputMap[A];
      error?: never;
    }
  | {
      ok: false;
      version: number;
      action_id: A;
      execution_id: string;
      result?: never;
      error: ActionError;
    };

export type ActionCallOptions = {
  confirmed?: boolean;
  executionId?: string;
  surface?: string;
};

export type RawActionInvoker = (request: {
  action_id: string;
  input: unknown;
  confirmed: boolean;
  execution_id?: string;
  surface?: string;
}) => Promise<unknown>;

export function createActionClient(invoker: RawActionInvoker, defaults: { surface?: string } = {}) {
  return async function call<A extends ActionId>(
    actionId: A,
    input: ActionInputMap[A],
    options: ActionCallOptions = {},
  ): Promise<ActionEnvelope<A>> {
    const surface = options.surface ?? defaults.surface;
    const request: ActionRequest<A> = {
      action_id: actionId,
      input,
      confirmed: options.confirmed ?? false,
      ...(options.executionId === undefined ? {} : { execution_id: options.executionId }),
      ...(surface === undefined ? {} : { surface }),
    };
    return (await invoker(request)) as ActionEnvelope<A>;
  };
}

export type TauriInvoke = <T>(
  command: string,
  args?: Record<string, unknown>,
) => Promise<T>;

export type TauriActionClientOptions = {
  command?: string;
  requestArgument?: string;
  surface?: string;
};

export function createTauriActionClient(
  invoke: TauriInvoke,
  options: TauriActionClientOptions = {},
) {
  const command = options.command ?? "action_parity_call";
  const requestArgument = options.requestArgument ?? "request";
  return createActionClient(
    (request) => invoke(command, { [requestArgument]: request }),
    { surface: options.surface ?? "gui" },
  );
}
