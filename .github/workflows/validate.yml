name: Validate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run validator tests
        run: npm test

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Test Rust registry and adapters
        run: cargo test --workspace

      - name: Generate and verify the Rust registry example
        run: |
          npm run generate:rust-example
          node bin/action-parity.mjs verify examples/rust-registry/generated/action-parity.json --out artifacts/rust-registry-evidence.json

      - name: Generate and verify the Node registry example
        run: |
          npm run generate:node-example
          node bin/action-parity.mjs verify examples/node-registry/generated/action-parity.json --plan examples/node-registry/generated/action-parity.verify.json --out artifacts/node-registry-evidence.json

      - name: Check clean installation and package contents
        run: npm run check:release

      - name: Validate examples
        run: npm run validate

      - name: Check documentation links
        run: npm run check:links

